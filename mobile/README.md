📍 Mobile - Localiza Fácil
Este diretório contém o aplicativo mobile em React Native + Expo.
Ele consome a API hospedada no Render, mas pode ser executado localmente para desenvolvimento.

⚙️ Como rodar localmente
- Entre na pasta do mobile:
cd mobile
- Instale as dependências:
npm install
- Inicie o Expo:
npx expo start
- No terminal, você verá opções para abrir o app em:
- Expo Go (no celular, escaneando o QR Code)
- Android Emulator
- iOS Simulator

📌 Observações
- Certifique-se de que o backend esteja rodando (localmente ou via Render).
- Para testar no celular, instale o aplicativo Expo Go e escaneie o QR Code gerado.
- O mobile está configurado para consumir a mesma API usada pelo frontend.
